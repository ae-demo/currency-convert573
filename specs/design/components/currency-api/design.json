{
  "name": "currency-api",
  "type": "service",
  "version": "0.1.0",
  "language": "Ballerina",
  "buildpack": "docker",
  "appPath": "currency-api",
  "entrypoint": "deployment/service",
  "exposure": "intranet",
  "stories": [
    1,
    2,
    3,
    4
  ],
  "description": "Ballerina service exposing the supported currency list and a conversion operation. Calls exchange-rate-service for the current rate on each conversion; does not persist conversions or rate history.",
  "dependencies": [
    {
      "kind": "platform-resource",
      "name": "user-auth",
      "description": "Verifies the gateway's signed assertion for every request from currency-webapp.",
      "resourceType": "thunder-app",
      "wiring": {
        "ref": "currency-convert5-daf943e0",
        "envBindings": {
          "client_id": "USER_AUTH_CLIENT_ID",
          "issuer": "USER_AUTH_ISSUER",
          "jwks_url": "USER_AUTH_JWKS_URL",
          "resource": "USER_AUTH_RESOURCE",
          "scopes": "USER_AUTH_SCOPES"
        }
      }
    },
    {
      "kind": "external",
      "name": "exchange-rate-service",
      "description": "Fetches the current exchange rate for the requested currency pair when converting an amount.",
      "wiring": {
        "ref": "currency-convert5-86b2cfcc",
        "envBindings": {
          "EXCHANGERATE_HOST_ACCESS_KEY": "EXCHANGERATE_HOST_ACCESS_KEY"
        }
      }
    }
  ],
  "exposesAPI": {
    "auth": "end-user-required"
  },
  "skillsPinned": [
    "openapi-conventions",
    "ballerina",
    "thunder-authentication",
    "api-management"
  ]
}
